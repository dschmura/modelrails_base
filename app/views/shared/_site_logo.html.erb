<%# locals: (name_class: "text-xl font-bold text-text-heading") %>
<%# The lockup: mark + wordmark as siblings, no wrapper — the caller's link is
    the flex box and sizes the mark with [&>svg]:h-*. The mark itself is the
    fork-owned shared/_site_mark (see /docs/developer/forking). %>
<%= render "shared/site_mark" %>
<span class="<%= name_class %>"><%= t("application.name") %></span>
