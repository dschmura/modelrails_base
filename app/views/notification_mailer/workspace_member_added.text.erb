<%= t("notification_mailer.workspace_member_added.heading", workspace_name: @workspace.name) %>

<%= t("notification_mailer.workspace_member_added.greeting", name: @recipient.first_name) %>

<%= t("notification_mailer.workspace_member_added.body", workspace_name: @workspace.name, new_role: @role.name) %>

<%= t("notification_mailer.workspace_member_added.action") %>: <%= workspace_url(@workspace) %>
