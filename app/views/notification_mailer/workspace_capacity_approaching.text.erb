<%= t("notification_mailer.workspace_capacity_approaching.heading", workspace_name: @workspace.name, metric: @metric) %>

<%= t("notification_mailer.workspace_capacity_approaching.greeting", name: @recipient.first_name) %>

<%= t("notification_mailer.workspace_capacity_approaching.body", workspace_name: @workspace.name, metric: @metric, current: @current, limit: @limit) %>

<%= t("notification_mailer.workspace_capacity_approaching.action") %>: <%= @settings_url %>
